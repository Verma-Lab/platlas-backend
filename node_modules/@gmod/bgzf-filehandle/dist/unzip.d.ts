interface VirtualOffset {
    blockPosition: number;
    dataPosition: number;
}
interface Chunk {
    minv: VirtualOffset;
    maxv: VirtualOffset;
}
export declare function unzip(inputData: Uint8Array): Promise<Uint8Array<ArrayBuffer>>;
export declare function unzipChunkSlice(inputData: Uint8Array, chunk: Chunk): Promise<{
    buffer: Uint8Array<ArrayBuffer>;
    cpositions: number[];
    dpositions: number[];
}>;
export {};
